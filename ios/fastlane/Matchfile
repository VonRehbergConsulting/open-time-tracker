git_url(ENV["MATCH_GIT_URL"] || "https://github.com/VonRehbergConsulting/ios-certificates.git")

storage_mode("git")

type("appstore")

app_identifier(ENV["IOS_APP_IDENTIFIER"] || "vonrehberg.timetracker")
username(ENV["APPLE_ID"] || "dshtangey@vonrehberg.consulting")
team_id(ENV["TEAM_ID"] || "383J7R89WY")